def func_eight(s, n):
    if len(s)> n:
        return s.upper()
    else:
        return s

"""Функция сравнивает длину строки с введнным числом.
 Если строка больше этого числа возвращает ее в верхнем регистре, иначе возвращает исходную строку"""
    
